---
labels: ['umd build task', 'app', 'app type']
description: 'A UmdBuildTask app component.'
---

A basic Bit app component.

## Run the app

```
# use the app
bit use umd-build-task

# run the app
bit run umd-build-task
```

## Compose the app

Compose the app to create your own instance of this app.

```ts
export default UmdBuildTask.from({});
```
